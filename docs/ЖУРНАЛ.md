# ЖУРНАЛ стройки Atlas

Формат записи: дата · часть · сделали · пробовали (грабли) · дальше.

## 05.09.2026 · допрос и план
- Сделали: допрос (4 пачки вопросов), утверждены все решения, написан `ПЛАН.md`, заведён репозиторий `Alexolimb/atlas-history`, паспорт в Brain.
- Разведка: границы — `aourednik/historical-basemaps` (54 снимка, GPL-3); Pixabay-музыку можно в платное приложение; Wikidata SPARQL открыт из браузера, лимит 60 с запросов в минуту на IP, в 2026 заметно медленнее; глобус — `react-globe.gl` (MIT).
- Дальше: часть 0 — каркас.

## 05.09.2026 · ✅ Часть 0 — каркас

**Сделали**
- Vite 6 + React 18 + TypeScript 5, шесть экранов и маршруты, `basename` из `BASE_URL`.
- Палитра «тёмный музей» и светлый «пергамент» — целиком в `src/styles/tokens.css`, в компонентах ни одного прямого цвета. Заголовки Cormorant Garamond, текст Inter (оба с кириллицей), шрифты кэшируются офлайн.
- Настройки: язык (30 штук), тема (тёмная/светлая/как в системе), размер текста, меньше движения, объёмный глобус, музыка и звуки (заглушены до части 7), прогресс.
- Прогресс в IndexedDB + перенос файлом «Сохранить / Загрузить», формат версионирован, чужой файл отвергается.
- PWA: манифест, service worker, офлайн проверен вживую — сервер выключен, приложение открылось и глубокая ссылка тоже.
- Иконки рисуются кодом (`npm run icons`): PNG 192/512/maskable, apple-touch, favicon.svg, atlas.ico для ярлыка. Ни одного бинарника руками.
- Ярлык на рабочем столе: `scripts/yarlyk.ps1` → Chrome в режиме отдельного окна.
- Выкладка: GitHub Actions → Pages, с воротами «сборка не пустая» (index/404/манифест/sw и минимум файлов).
- Ворота `npm run check`: типы + 28 тестов + сборка + бюджет размера. Сборка 320 КБ, JS 291 КБ при бюджете 1536 КБ.

**Пробовали (грабли)**
1. `defineConfig` из `vitest/config` в `vite.config.ts` роняет сборку: у vitest своя вложенная копия vite, типы плагинов двух копий не сходятся. Лечение — отдельный `vitest.config.ts` без плагинов.
2. `PageHead` берёт ключ `${ns}.sub`; забытый ключ не падает, а печатает на странице «settings.sub». Нашлось глазами на скриншоте. Заведён `tests/screens.test.ts` — он тут же поймал вторую такую же дырку (`sources.sub`).
3. Переключатель из трёх вариантов на флексе сваливался во вторую строку на 375 px. Переведён на сетку `1fr 1fr 1fr`.
4. Подписи в профиле были захардкожены по-русски мимо i18n — переведены на ключи `profile.stats.*`.
5. `.claude/launch.json` читается из корня `C:\`, а не из папки проекта — конфигурации `atlas-dev` и `atlas-prod` добавлены туда.
6. Не вешаемся на системный `prefers-reduced-motion`: у Алекса анимации Windows выключены, и приложение молча стало бы неподвижным. Слушаем только настройку внутри приложения.

**Проверено вживую**
Тёмная и светлая темы (фон и `theme-color` меняются) · язык `ar` → интерфейс падает на английский и `dir=rtl` · настройки переживают перезагрузку (IndexedDB) · офлайн при выключенном сервере, включая прямую ссылку `/sources` · ярлык на столе читается и указывает на Chrome.

**Дальше:** часть 1 — данные (Wikidata: ~500 сущностей ядра, метки на 30 языках, онлайн-догрузка с кэшем).

## 05.09.2026 · ✅ Часть 1 — данные из Wikidata

**Сделали**
- Пайплайн `scripts/data/`: отбор Q-номеров запросами по объективному признаку (число разделов Википедии), полные данные через wbgetentities, разбор в свою форму, запись `public/data/core/`.
- Ядро: 835 карточек — 340 государств (все члены ООН + исчезнувшие империи), 381 человек, 77 событий, 37 династий. 9800 связей. Имена и описания на 30 языках, 7531 имя на русском.
- Догрузка из сети по требованию (`src/lib/wikidata.ts`) + кэш на устройстве с сроком годности (`src/lib/cache.ts`). Тексты статей и лицензии картинок (`src/lib/wikipedia.ts`).
- Ворота `npm run data:check` — 20 проверок, включая опорные факты: Цезарь −100…−44, Александр −356…−323, Наполеон 1769…1821, СССР 1922…1991, Древний Рим −753…476.
- Живая проверка источников `npm run data:smoke` (в ворота НЕ входит: профилактика у Wikipedia не должна ронять сборку).

**Пробовали (грабли)**
1. **Кириллица в заголовке HTTP роняет каждый запрос.** В User-Agent было русское описание — `fetch` падал ещё до выхода в сеть, с сообщением про ByteString. Заголовки умеют только латиницу.
2. **Широкий запрос SPARQL не досчитывается.** «Все люди с должностью» и `VALUES` из девяти занятий обрывали соединение. Лечение: один запрос — один узкий класс; правители берутся не запросом, а из свойств самих государств.
3. **Ловушка `mul`.** С 2024 года Wikidata хранит имена, одинаковые во всех языках латиницы, ОДИН раз под кодом `mul` и удаляет их из английского. Кто просит только `en`, не находит имени у Эйнштейна, Дарвина, Трампа — и не замечает: ошибки нет, просто пусто. Просим `mul` и подставляем его.
4. **Ключ кэша не учитывал набор языков** — после добавления `mul` старые ответы читались бы дальше и вернули бы ту же пустоту. Добавлена метка версии в ключ.
5. **Известность упиралась в 30.** Считали число разделов Википедии с фильтром по нашим 30 языкам — счётчик всегда упирался в 30 и переставал различать. Настоящий счёт берётся отдельным запросом без фильтра.
6. **Заголовки статей на 30 языках раздували общий файл вдвое.** Перенесены в файлы имён по языкам: приложение грузит один язык, а не тридцать.

## 05.09.2026 · ✅ Часть 2 — справочник

**Сделали**
- Экран справочника: поиск (сначала по устройству, потом дописывает из Wikidata), фильтры по видам, закладки.
- Карточка `/e/:id` для страны, человека, события и династии. Картинка с автором и лицензией, выжимка статьи со ссылкой, кнопка «В закладки», кнопка «Назад».
- **Лента правителей.** Собирается из двух источников, потому что Wikidata описывает это по-разному: у современных стран список глав лежит на самой стране, у древних — наоборот, у каждого человека висит должность, привязанная к государству. Без второго источника Древний Рим и половина мира остались бы без правителей вовсе.
- **Семейное древо** в три ряда: родители → человек и супруги → дети, плюс братья и сёстры. Любой в древе кликабелен: нажал на отца — древо перестроилось вокруг него, и по семье можно идти вглубь сколько угодно.
- Внизу каждой карточки — ссылки на Wikidata и Википедию.

**Проверено вживую**
Справочник: 120 карточек в списке, поиск находит · Древний Рим: 34 записи ленты, у Цезаря видны все должности с годами · Наполеон: две жены, дети, восемь братьев и сестёр, портрет Давида с подписью «Public domain» · СССР: 54 правителя · **офлайн: сервер выключен, карточка Древнего Рима открылась целиком с лентой правителей**.

**Пробовали (грабли)**
1. Заголовок ленты сначала гласил «Правители», а показывал ещё и квесторов с эдилами. Переименован в «Правители и должностные лица» — говорить надо то, что есть.
2. Портрет обрезался `object-fit: cover` и у вертикальных картин отрезал голову. Заменено на `contain`.

**Дальше:** часть 3 — глобус с ползунком времени.

## 05.09.2026 · ✅ Часть 3 — глобус с ползунком времени

**Сделали**
- 52 карты мира от −10000 до 2010 (`aourednik/historical-basemaps`, GPL-3). Каждая прорежена своим алгоритмом Рамера — Дугласа — Пекера: сырые файлы по 3–20 МБ ужаты до 8–144 КБ, форма стран узнаваема. Всего 3,3 МБ вместо сотен.
- Объёмный глобус (`react-globe.gl`) с золотой атмосферой. Цвет страны выводится из её имени, поэтому один и тот же цвет держится во все годы — двигая ползунок, видно, как ИМЕННО ЭТА страна росла и сжималась.
- Плоская карта на обычном SVG как запасной вид: без библиотек, работает на слабых устройствах.
- Ползунок ходит по срезам карт, а не по годам: каждое положение — настоящая карта, а не выдуманная середина. Плюс быстрые метки: Античность, Рим, Средние века, Открытия, Империи, Войны, Сегодня.
- Касание страны → её карточка. Связка «подпись на карте → Q-номер» строится отдельным скриптом.
- Код глобуса (внутри three.js, 1,9 МБ) грузится отдельным куском только при заходе на главный экран. Пока он едет, показывается плоская карта — не чёрный прямоугольник с надписью «загружаю».
- На слабом устройстве (мало памяти, мало ядер, экономия трафика, медленная сеть) объёмный глобус по умолчанию выключен. Переключатель в настройках остаётся.

**Как связывали страны с карточками (и почему так осторожно)**
Открыть по нажатию на Русь карточку Румынии хуже, чем не открыть ничего: человек прочитает неправду и не поймёт этого. Поэтому связываем только при точном совпадении имени — сначала с английскими именами ядра, потом с именами и прозвищами в Wikidata, и всегда отсекая заведомо не-страны по описанию («порода утки», «фильм», «песня»). Связалось 1401 подпись из 2298. По картам: 2010 год — 98%, 1914 — 95%, 1500 — 69%, 500 — 59%, −1 — 52%. Несвязанное — это в основном сборные подписи вроде «Islamic city-states» и «Mossi States», у которых одной карточки и не должно быть. Такая страна на глобусе всё равно видна и подписана, просто не открывается.

**Пробовали (грабли)**
1. **Глобус раздувал страницу до бесконечности.** Наблюдатель мерил контейнер → canvas рос → контейнер рос → canvas рос ещё. Страница уезжала вбок на телефоне. Лечение: размер задаёт только внешний блок через `aspect-ratio`, а глобус лежит внутри absolute-слоем и раздвинуть родителя не может. Плюс округление ширины вниз — дробные значения сами по себе гоняют пересчёт по кругу.
2. **«Франция в подчинении: Франция».** У независимых стран поле «в подчинении» повторяет собственное имя. Держим его, только если там ДРУГАЯ страна.
3. **Первая проверка связей искала «Muscovy», а на карте написано «Tsardom of Muscovy».** Проверять надо по тем именам, что реально есть в данных, а не по тем, что кажутся.
4. **Прозвища.** Строгое совпадение с основным именем оставляло без карточек СССР («USSR») и Московское царство. Теперь принимаем и совпадение с прозвищем — но только если Wikidata сама сказала, что попала именно в прозвище. Связность выросла с 55% до 61%.
5. **Бюджет размера считал не то.** Он складывал весь JS, включая глобус, который грузится по требованию, — и запрещал сборку. Переписан: первая загрузка берётся из `index.html` (теги script и modulepreload), остальное считается отдельно. Первая загрузка — 295 КБ.
6. `.at()` у массивов требует библиотеки ES2022 в настройках TypeScript; для импорта `.mjs` в тест нужен файл описания типов с расширением `.d.mts`, а не `.d.ts`.

**Проверено вживую**
Глобус крутится, страны эпохи раскрашены · метка «Рим» переносит в 100 год, 83 страны · плоская карта: 79 стран, тап открывает панель · страница по ширине не уезжает · связи загружаются, Рим → Q2277, СССР → Q15180.

**Дальше:** часть 4 — путь по эпохам.

## 05.09.2026 · ✅ Часть 4a — путь: формат главы и эпоха античности

**Сделали**
- Формат главы: файл с шапкой в `content/<язык>/e<эпоха>/c<номер>.md`. Описание — `content/README.md`.
- **Ворота честности `npm run content`.** Каждая дата, названная в главе, вынесена в раздел `facts` и сверяется с Wikidata. Разошлось хоть на год — сборка падает, глава в приложение не попадает. Плюс проверки: обе языковые версии на месте, шапки совпадают, вопросы викторины опираются только на проверенные факты.
- Четыре главы эпохи «Классическая античность» на русском и английском: Греция и Александр · Римская республика · Индия Маурьев · Китай: Цинь и Хань. 2235 слов по-русски.
- Экран пути: 12 эпох, у каждой свои главы, пройденные помечены галочкой. Ничего не заперто — открыть можно любую главу в любой момент, как и договаривались.
- Экран главы: рассказ с подзаголовками, карточки справочника, ссылка на глобус в нужный год, переходы «предыдущая / следующая».
- **Проверка после главы.** Варианты ответов НЕ выдуманы: правильный берётся из карточки Wikidata, а неправильные — настоящие даты и имена других карточек. Вопрос не может оказаться неверным сам по себе.
- Опыт: 60 за главу, 15 за верный ответ.

**Что поймали ворота честности (и это главное)**
Проверка фактов нашла в моих же текстах пять ошибок ещё до того, как их увидел человек:
1. Платон родился в −427, а не в −428.
2. **Q7350 — это Панамский канал, а не династия Цинь.** Глава про древний Китай ссылалась бы на канал.
3. **Q7462 — империя Сун (960–1279), а не Хань.** Ошибка в тысячу лет.
4. Начало Хань в Wikidata — −206, а не −202.
5. Конец империи Маурьев — −185, а не −184.
Без этих ворот все пять уехали бы к человеку как «проверенная история».

**Пробовали (грабли)**
1. **Прогресс терялся при переходе между экранами.** Хранилище асинхронное: в первые мгновения после запуска в памяти лежит ПУСТОЙ прогресс, а вызов «отметить сегодняшний день» тут же записывал эту пустоту на устройство — вместе с ней стирались опыт и пройденные главы. Нашлось глазами: глава была засчитана, а после перехода на карточку от неё не осталось следа. Лечение двойное: ждём чтения перед первой записью и, на всякий случай, само хранилище отказывается писать, пока не прочитано. Заведён тест.
2. Разбор шапки ломался о запятую внутри текста вопроса: «после битвы, которую сам выиграл». Разбор научен не резать по запятым внутри кавычек.
3. Ссылки на людей в шапке надо брать не по памяти, а проверять: две из четырёх глав ссылались не на те карточки (см. выше).

**Проверено вживую**
Путь: 12 эпох, 4 главы, «Пройдено 0 из 4» · глава про Рим: 15 абзацев, 3 подзаголовка, карточки Цезаря и Цицерона · викторина: «В каком году убили Юлия Цезаря?» с вариантами 44 до н. э. / 323 до н. э. / 210 до н. э. / 14 — все настоящие даты из справочника · после ответов «Верно 1 из 3», +75 XP · профиль: уровень 1, 75 из 500 опыта, 1 глава · прогресс переживает переходы между экранами.

**Дальше:** часть 4b — остальные 11 эпох (~38 глав), это работа по готовому шаблону.

## 05.09.2026 · ✅ Часть 5 — игровой слой и повторение

**Сделали**
- **Интервальное повторение** по алгоритму SM-2 (тот же, что в Anki): вспомнил легко — вернём нескоро, забыл — завтра снова. Карточки заводятся сами на всё, что человек открыл в справочнике; спрашиваются только настоящие даты из Wikidata.
- Экран повторения: вопрос, кнопка «Показать ответ», три честные кнопки самооценки. Приложение не устраивает экзамен, оно помогает не забыть.
- **Двенадцать достижений**, все считаются из настоящего прогресса: главы, карточки, серии дней, уровни, закладки. За вход не хвалим — такая похвала обесценивает остальные.
- **Тайна дня**: одна известная личность на сутки, выбирается по дате, поэтому у всех сегодня одна и та же, а завтра будет другая.
- Профиль переписан: уровень с полосой опыта, серия, главы, карточки, ссылка на повторение, тайна дня, стена достижений.

## 05.09.2026 · ✅ Часть 7 — музыка и звуки

**Сделали**
- Плеер фоновой музыки и тихий шелест при перелистывании. По умолчанию всё выключено, как и договаривались; включается только руками в настройках. Заглушки «Часть 7» с настроек сняты.
- Ворота размера теперь считают и музыку (предел 12 МБ).

**Чего не вышло и как решено**
Скачать треки с Pixabay из сборочного скрипта не получилось: ссылки на сами файлы отдаёт их скрипт в браузере, в разметке страницы их нет. Подставлять вместо них случайные ссылки с других сайтов — значит тащить чужую лицензию в приложение, которое может стать платным. Поэтому сделано так:
1. **Свой фон, собранный в браузере.** Пять тихих голосов, медленно расходящихся по высоте, сквозь мягкий фильтр. Ударных и мелодии нет — только ровное дыхание на заднем плане. Весит ноль байт, работает офлайн, играет прямо сейчас.
2. **Файлы человека главнее.** Появились треки в `public/audio/` и `tracks.json` — играют они, свой фон отключается сам. Инструкция на пять минут — `public/audio/README.md`.
Звук перелистывания синтезируется так же: короткий отфильтрованный шум, похожий на страницу книги.

**Проверено вживую**
Повторение: две карточки Наполеона, вопрос «В каком году родился?» · настройки: ни одной заглушки, ничего не заблокировано · музыка включается, звуковой контекст запускается и играет, выключается плавно.

**Дальше:** часть 6 — ИИ-гид, часть 4b — остальные главы, часть 8 — полировка.

## 05.09.2026 · 🟡 Часть 6 — ИИ-гид (приложение готово, сервер ждёт Алекса)

**Сделали**
- Панель гида под каждой карточкой и главой. Три кнопки вместо поля ввода: «объясни проще», «почему это важно», «что дальше». Свободный чат сделал бы приложение «спроси у модели», и проверенный справочник стал бы не нужен.
- Клиент с жёстким договором: таймаут, молчание без интернета, и **обязательная подпись ответа**. Воркфлоу обязан вернуть `app: "atlas"`; неподписанный ответ приложение не показывает.
- Воркфлоу n8n готов файлом: `n8n/atlas-guide.json`. В задании модели трижды записано отвечать только по открытому тексту и прямо запрещено называть даты, которых в нём нет.
- Пока воркфлоу не поднят, гид честно пишет «пока недоступен», и приложение работает целиком без него — проверено вживую.

**Почему подпись обязательна.** В NEXUS адрес однажды указывал на воркфлоу *другого* приложения, и «наставник по коду» бодро учил инвестициям. Заметили глазами. Подпись делает такую подмену видимой сразу.

**От Алекса нужен один шаг:** импортировать `n8n/atlas-guide.json` и включить. Инструкция — `n8n/README.md`.

## 05.09.2026 · 🟡 Часть 4b — путь дорос до трёх эпох

Добавлены главы: «Шумер и рождение письменности» (эпоха 2), «Римская империя и новая вера» (эпоха 5), «Две мировые войны» (эпоха 12). Всего 7 глав, 3847 слов по-русски и столько же по-английски.

Ворота фактов снова поймали ошибки: начало Древнего Египта в Wikidata — −4000 (от додинастического периода), а не −3150 (объединение); текст главы приведён в соответствие. И сами ворота были неправы: у войн и империй даты лежат в свойствах «время начала/конца», а не «дата основания», — цепочка свойств выправлена под ту же, что в сборке справочника.

Остальные 9 эпох ждут глав. Работа механическая: шаблон, ворота и все экраны готовы, писать остаётся только текст.

## 05.09.2026 · ✅ Часть 9 — ревизия

**Обход девяти экранов на телефонном размере (375 px):** ни одной страницы, уезжающей вбок · ни одного непереведённого ключа на экране · ни одного голого Q-номера · ни одной красной ошибки.

**Доступность:** у всех кнопок и ссылок есть имя, у картинок — подпись, у полей — метка, на странице ровно один заголовок первого уровня, язык страницы проставляется.

**Контраст замерен, а не прикинут.** Тёмная тема была в порядке (6,4–15,7 при пороге 4,5). **Светлая — нет: ссылки давали 2,6, их почти нельзя было прочитать.** Золото, красивое на тёмном фоне, на пергаменте не работает. Палитра светлой темы затемнена, замер повторён: текст 15,1 · приглушённый 6,3 · ссылки 7,3 · золото 5,4.

Ворота: 148 тестов, первая загрузка 324 КБ, все бюджеты в порядке.

## 05.09.2026 · ✅ Часть 8 — тридцать языков интерфейса

**Сделали**
- Переведены все 214 строк интерфейса на 28 языков: испанский, китайский, хинди, арабский, португальский, французский, немецкий, японский, корейский, итальянский, турецкий, польский, украинский, нидерландский, вьетнамский, индонезийский, тайский, персидский, шведский, чешский, греческий, иврит, румынский, венгерский, датский, финский, норвежский, бенгальский.
- Русский и английский остались внутри кода (язык по умолчанию и запасной), остальные 28 приезжают файлом из `public/locales/` при выборе. Класть все в первую загрузку значило бы заставить каждого качать 324 КБ чужих языков.
- **Даты теперь на языке человека.** Раньше `formatHistDate` знал только русский и английский, и японская карточка Цезаря показывала «July 100 BC». Теперь ru и en остаются написанными вручную (у них свои обороты: «5 век до н. э.», «1920-е»), а остальные 28 форматирует сам браузер — он лучше меня знает, что «до нашей эры» по-японски 紀元前, а на иврите לפנה״ס.
- Календарь принудительно григорианский, цифры обычные: тайская локаль по умолчанию показала бы буддийский год, и человек увидел бы «2332» там, где во всём приложении стоит «1789».
- Неточная дата на этих языках помечается знаком «≈» — приблизительное не выдаётся за точное.

**Ворота**
Новый тест `tests/locales.test.ts` — 143 проверки: у каждого из 28 файлов тот же набор ключей, что у русского, ни одной пустой строки, все подстановки вида `{{count}}` на месте, и ни одной забытой русской фразы. На двух языках забытый ключ ловится глазами, на тридцати — уже нет.

**Проверено вживую**
Переключение на японский, арабский, украинский и тайский: меняются меню, заголовки и подписи; у арабского страница разворачивается справа налево. Карточка Цезаря по-японски: имя, вид, описание и заголовки разделов — всё на японском.

Всего 298 тестов. Первая загрузка приложения — 325 КБ.

## 05.09.2026 · 🟡 Часть 4b — путь дописан до всех двенадцати эпох

**Сделали**
Девять новых глав на русском и английском. Теперь **в каждой из двенадцати эпох есть содержание**, пустых больше нет. Всего 16 глав, 8338 слов по-русски и столько же по-английски.

| Эпоха | Глава |
|---|---|
| 1 | Как люди перестали кочевать — земледелие как ловушка, Гёбекли-Тепе, Чатал-Хююк без начальства |
| 3 | Ассирия и Персия — две империи, два способа удержать чужие народы |
| 6 | Византия и рождение ислама — «тёмные века» были тёмными только для одного угла карты |
| 7 | Монголы и связанный мир — первая глобализация, и по ней же приехала чума |
| 8 | Чёрная смерть и то, что после — как катастрофа сделала выживших свободнее |
| 9 | Колумб и Реформация — печатный станок как общая причина обоих |
| 10 | Короли, чиновники и здравый смысл — абсолютизм и Просвещение |
| 11 | Революция и машина — равенство перед законом и паровой двигатель в одном веке |
| 12 | Холодная война и мир после неё |

**Ворота фактов снова поймали ошибки**: конец Чёрной смерти в Wikidata — 1352, а не 1353; начало холодной войны — 1945, а не 1947. Текст глав приведён в соответствие, а не подогнан задним числом.

**Отбор Q-номеров снова показал, зачем проверять.** Первый поиск по слову «Assyria» даёт римскую провинцию, «Babylonia» — род моллюсков и видеоигру, «Charlemagne» — город в Квебеке. Ни один Q-номер в главах не взят по памяти.

**Дальше:** вторые и третьи главы эпох. Работа механическая: шаблон, ворота и все экраны готовы.

## 05.09.2026 · ✅ Ревизия: главный экран уезжал вбок на телефоне

**Нашли настоящий баг, который до этого прятался за инструментом проверки.**

Полоса быстрых меток эпох («Античность · Рим · Средние века…») — семь кнопок в строку, которым нужно 720 px. Элементы флекса и сетки по умолчанию отказываются сжиматься меньше своего содержимого, поэтому эта строка растягивала колонку сетки, а вместе с ней **всю страницу главного экрана** до 860 px. На телефоне 360 px страница уезжала вбок, и это был самый первый экран, который человек видит.

Лечение: `min-width: 0` по всей цепочке — на элементах сетки экрана, на панели времени и на строке меток. Полоса меток и так прокручивается, сжимать её безопасно. Плюс сцене глобуса задана явная ширина: связка `aspect-ratio` и `max-height` без неё считает высоту первой и подгоняет ширину под неё.

**Отдельный урок про проверку.** Эмуляция телефона в панели браузера меняла только цифры `innerWidth`, а раскладка оставалась на 876 px — из-за этого предыдущая ревизия отрапортовала «ни одной страницы, уезжающей вбок». Настоящий ответ дал iframe шириной 360 px: у него настоящее окно просмотра. **Если инструмент проверки говорит «всё хорошо», стоит убедиться, что он вообще меряет то, что нужно.**

После починки перемерено в честном окне 360 px: все десять экранов ровно 360, ни одного выхода за край.
